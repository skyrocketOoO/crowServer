#include <string>

template <typename T>
std::string validate(T obj) {
    // const auto view = rfl::to_view(obj);

    // if (*view.get<"name">() == "Maggie"){
    //   return "name not empty";
    // }

    return "";
}